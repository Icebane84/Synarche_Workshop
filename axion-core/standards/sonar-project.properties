# -----------------------------------------------------------------------------
# PHOENIX SYNARCHE PROTOCOL - SONARQUBE CONFIGURATION
# -----------------------------------------------------------------------------
#
# Domain: GVRN Evolution: Omega Ascension Signal: OMEGA
#
# Genesis Stamp: 2026-02-21 | Domain: GVRN | State: [ACTIVE] | Tags: OGLN_v13, SONAR, AUDIT | Criticality: Core
# -----------------------------------------------------------------------------
# [ARTIFACT START]
#
# Block A: The Identification Lock (UIP-V13)
# -----------------------------------------------------------------------------
# Artifact ID         : CFG-SONAR-001_SonarQubeConfig
# Official Name       : sonar-project.properties
# Version             : v13.1 [OMEGA]
# Domain              : GVRN
# Celestial Class     : [STAR]
# Evolution           : Omega Ascension
# Status              : [ACTIVE]
# Relations           : GOVERNED_BY: CORE-CODEX-001
# -----------------------------------------------------------------------------

# --- Project Identity ---
sonar.projectKey=Synarche.Protocol
sonar.projectName=The Synarche Protocol
sonar.projectVersion=13.1.0-Omega

# --- Scan Scope (The Weave) ---
# We explicitly include the active cores, agents, and the shared source spine.
sonar.sources=axion-core/src,src/agents,axion-core/tools,nova_forge

# --- Exclusions (The Pruning) ---
# Remove noise, binaries, logs, and temporary playground data.
sonar.exclusions=**/node_modules/**, \
                 **/venv/**, \
                 **/.git/**, \
                 **/__pycache__/**, \
                 **/dist/**, \
                 **/.pytest_cache/**, \
                 **/_logs/**, \
                 **/_archive/**, \
                 **/Playground/**, \
                 **/Open\ Notebook/**, \
                 **/*.min.js, \
                 **/*.css

# --- Tests (The Verification) ---
sonar.tests=axion-core/tests
sonar.test.inclusions=**/tests/**, \
                      **/*_test.py, \
                      **/*.test.ts, \
                      **/*.spec.ts

# --- Language Specifics ---
sonar.sourceEncoding=UTF-8
sonar.python.version=3.10
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# --- Quality Gate ---
# Enforce the "High-Standard" environment.
# sonar.qualitygate.wait=true

# -----------------------------------------------------------------------------
# Block D: Standardized Synergy Block (The Loom Signature)
# -----------------------------------------------------------------------------
# Synergistic Artifact ID | Relationship Type | Synergistic Impact
# CORE-CODEX-001          | GOVERNS           | The Codex provides the Supreme Law for this artifact.
# GVRN.Registry.Master    | INDEXES           | This artifact is indexed in the Master Registry.
#
# [ARTIFACT END]
# -----------------------------------------------------------------------------
