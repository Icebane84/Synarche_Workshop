# **[DOC-STD-001] Synarche Universal Coding Standards (SUCS)**

### **Metadata Block**

| Field              | Value                                 |
| :----------------- | :------------------------------------ |
| **Artifact ID**    | DOC-STD-001                           |
| **Classification** | **GOVERNANCE / MANDATORY**            |
| **Version**        | 2.1.0 (Catalyst Infused)              |
| **Date Created**   | 2025-11-19                            |
| **Last Modified**  | 2025-12-02                            |
| **Location**       | Bellefontaine, Ohio                   |
| **Author**         | Synarche (User + Gemini)              |
| **Target Systems** | OGLN, Nova Forge, Axion-Core, PRS-001 |
| **Dependencies**   | UMB (Blueprints), AOP (Playbooks)     |

---

### **1.0 Operational Mandate & Architecture**

#### **1.1 The Mandate**

**What:** This document establishes the non-negotiable syntax, structure, and documentation rules for all software
generated by the Synarche. **How:** It is enforced via "Preventative QA" (Axion-Core) and manual review. Every line of
code must pass these standards before being committed to the Master Branch. **Why:** To ensure "Functional Coherence."
Code is not just logic; it is a knowledge graph. Standardization ensures that the AI (AISTF) can read, understand, and
refactor the codebase recursively without losing context.

#### **1.2 The Documentation Architecture**

To achieve high-fidelity code governance, we utilize a three-tiered documentation structure:

1. **The Universal Module Blueprint (UMB) – For Code Structure**
   - **Purpose:** Enforces "Architecture First" thinking. Defines Inputs, Outputs, Dependencies, and Error Handling
     before coding begins.
   - **Role:** Prevents "spaghetti code" and ensures every component has a defined contract within the Hybrid Knowledge
     Graph (HKG).

2. **The Operational Playbook (AOP) – For Development Workflows**
   - **Purpose:** Defines _how_ we write, test, and deploy code (Git standards, versioning, testing).
   - **Role:** Maintains velocity without sacrificing stability, ensuring AISTF can parse version history.

3. **The Relational Naming Convention (RNC) – For Semantic Consistency**
   - **Purpose:** A dictionary of reserved terms and naming patterns.
   - **Role:** Reduces cognitive load; variable names instantly reveal type, scope, and purpose.

---

### **2.0 Core Principles**

> **Rule 1: Explicit over Implicit.** Type hinting (Python) and Interface definitions (TypeScript) are mandatory. **Rule
> 2: Self-Documenting Code.** Variable names must be descriptive enough to render comments redundant for simple logic.
> **Rule 3: The "Why" Comment.** Comments should explain _why_ a decision was made, not _what_ the code is doing.

---

### **3.0 The Relational Naming Convention (RNC)**

_This section defines how entities are named to ensure instant recognition of their scope and function._

#### **3.1 General RNC Rules**

- **No Magic Numbers:** All operational constants must be named variables (e.g., `MAX_RETRY_ATTEMPTS = 3` instead of
  just `3`).
- **Boolean Predicates:** Boolean variables must answer a question (e.g., `is_active`, `has_permission`, `can_execute`).

#### **3.2 Language-Specific RNC**

| Entity Type            | Python Style      | TypeScript Style  | Example                       |
| :--------------------- | :---------------- | :---------------- | :---------------------------- |
| **Variables**          | `snake_case`      | `camelCase`       | `user_id` / `userId`          |
| **Functions**          | `snake_case`      | `camelCase`       | `calculate_velocity()`        |
| **Classes**            | `PascalCase`      | `PascalCase`      | `OrbitCalculator`             |
| **Constants**          | `SCREAMING_SNAKE` | `SCREAMING_SNAKE` | `DEFAULT_TIMEOUT`             |
| **Files (Logic)**      | `snake_case.py`   | `camelCase.ts`    | `data_loader.py` / `utils.ts` |
| **Files (Components)** | N/A               | `PascalCase.tsx`  | `UserProfile.tsx`             |
| **Interfaces**         | N/A               | `PascalCase`      | `UserConfig`                  |

---

### **4.0 Language Protocols**

#### **4.1 Python (The Backend Logic)**

- **Standard:** PEP 8 Extended.
- **Type Hinting:** **MANDATORY.** All function signatures must utilize the `typing` module (List, Dict, Optional, Any).
  - _Bad:_ `def process_data(data):`
  - _Good:_ `def process_data(data: Dict[str, Any]) -> List[int]:`
- **Docstrings:** Must follow the **Google Style Guide**. Must be present for all public methods and classes.
- **Imports:** Sorted alphabetically. Standard library first, third-party second, local application third.

#### **4.2 TypeScript (The Interface & Architecture)**

- **Standard:** Airbnb Style Guide (Adapted).
- **Strict Mode:** The `tsconfig.json` must have `"strict": true`.
- **Explicit Any:** Usage of `any` is **FORBIDDEN** unless explicitly documented with a `// TODO: Fix implicit any`
  comment. Use `unknown` or define a Generic `<T>` instead.
- **Interfaces vs Types:** Use `interface` for defining public API contracts (e.g., Data Models). Use `type` for unions
  and intersections.
- **Naming:** Interfaces should NOT be prefixed with `I` (e.g., `UserEntity`, not `IUser`).

---

### **5.0 Documentation & Knowledge Graph Integration**

#### **5.1 The "Why" Rule (Expanded)**

Comments must explain the _intent_, not the _mechanism_.

- _Forbidden:_ `x = x + 1 # Increment x`
- _Required:_ `retry_count += 1 # Increment to track connection attempts against MAX_RETRY limit`

#### **5.2 Artifact Linking**

When code implements a specific blueprint or governance document, it must reference the Artifact ID in the file header.

**Python Example:**

```python
# Reference: UMB-IDE-EXT-001
# Context: Implements the parser logic for the Axion Core
```

---

### **6.0 Version Control & Commit Standards**

#### **6.1 Commit Messages**

We utilize **Conventional Commits** to ensure the **AISTF** can parse our history.

- Structure: `<type>(<scope>): <subject>`
- Types:
  - `feat`: A new feature
  - `fix`: A bug fix
  - `docs`: Documentation only changes
  - `refactor`: A code change that neither fixes a bug nor adds a feature
  - `chore`: Maintenance (build tasks, package manager configs)
- _Example:_ `feat(parser): implement UMB JSON schema validation`

---

### **7.0 Enforcement**

These standards are not suggestions; they are requirements for the **Definition of Done (DoD)**. Code that violates
DOC-STD-001 is considered "Technical Debt" immediately upon creation and must be rejected during the review phase.

---

### **8.0 Catalyst Prompt Packet (Actionable Logic)**

| Prompt ID       | Catalyst Prompt                                            | Synergistic Goal                                                                                                   |
| :-------------- | :--------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------- |
| **CPP-STD-001** | `CMD: LintCode --target: [File] --standards: DOC-STD-001`  | **Automated Compliance:** Scanning code for syntax, magic numbers, and missing type hints using the defined rules. |
| **CPP-STD-002** | `CMD: VerifyCommitMsg --msg: [Message_String]`             | **History Parsing:** Ensuring the Git persistence layer matches the Conventional Commits specification.            |
| **CPP-STD-003** | `CMD: CheckTypeSafety --lang: [TS/PY] --strict_mode: true` | **Type Integrity:** Enforcing Strict Mode for TypeScript and Mandatory Type Hinting for Python.                    |

---

### **Appendix A: Language Philosophy**

Python and TypeScript are both powerful programming languages, but they differ fundamentally in their design, primary
use cases, and how they handle data types. In essence, **Python is a dynamically-typed language prized for its
simplicity and versatility**, particularly in backend development, data science, and scripting. In contrast,
**TypeScript is a statically-typed superset of JavaScript designed for building large-scale, robust web applications**,
with a strong emphasis on front-end development.

#### **A.1 Core Distinctions: Typing, Syntax, and Philosophy**

The primary difference between Python and TypeScript lies in their approach to **type systems**. This fundamental
distinction influences their syntax, error-handling capabilities, and overall development workflow.

- **What:** Python employs **dynamic typing**, where the type of a variable is checked at runtime. TypeScript, on the
  other hand, uses **static typing**, where variable types are checked at compile-time.
- **How:** In Python, you can assign different data types to the same variable without explicit declaration. The
  interpreter infers the type during execution. TypeScript requires you to declare the data type of a variable, and the
  compiler will flag any inconsistencies before the code is run. This is a core feature that it adds to JavaScript.
- **Why:** Python's dynamic typing allows for rapid prototyping and more concise code. TypeScript's static typing
  provides a safety net, catching potential errors early in the development process, which is especially beneficial in
  large and complex applications.

In terms of **syntax**, Python is known for its readability and use of whitespace to define code blocks. TypeScript's
syntax is very similar to JavaScript, using curly braces for code blocks.

**Python Example:**

```python
def greet(name):
    print("Hello, " + name)
```

**TypeScript Example:**

```typescript
function greet(name: string): void {
  console.log("Hello, " + name);
}
```

#### **A.2 Ecosystem and Common Use Cases**

The ecosystems surrounding Python and TypeScript are vast and tailored to their respective strengths, leading to
distinct areas of application.

- **What:** Python has a mature and extensive ecosystem of libraries and frameworks for a wide array of domains.
  TypeScript's ecosystem is built upon the vast JavaScript ecosystem, with strong support from modern front-end
  frameworks.
- **How:** Python's strength lies in libraries like **NumPy, Pandas, and TensorFlow for data science and machine
  learning**, and frameworks like **Django and Flask for backend web development**. TypeScript integrates seamlessly
  with front-end frameworks such as **Angular, React, and Vue.js**, and is widely used with **Node.js for building
  scalable server-side applications**.
- **Why:** The choice between Python and TypeScript often comes down to the specific needs of a project. For
  data-intensive applications, scientific computing, and rapid backend development, Python's ecosystem is unparalleled.
  For building interactive user interfaces and large-scale, maintainable web applications, TypeScript's ecosystem and
  its integration with JavaScript are ideal.

#### **A.3 Performance and Scalability**

While both languages can be used to build high-performing applications, their underlying architecture and typing systems
lead to different performance characteristics.

- **What:** Generally, TypeScript (when executed as JavaScript by the V8 engine) can have a performance edge over
  Python, especially in I/O-heavy situations.
- **How:** TypeScript's static typing can lead to more optimized code execution. Additionally, the asynchronous,
  non-blocking nature of JavaScript/TypeScript is well-suited for handling many concurrent operations, a common
  requirement in web applications. Python's Global Interpreter Lock (GIL) can be a limitation for true parallelism in
  multi-threaded applications, although it excels in single-threaded performance and can leverage multi-processing.
- **Why:** For applications requiring high concurrency, such as real-time web servers or complex user interfaces,
  TypeScript's performance model is often more advantageous. Python's performance is more than adequate for many backend
  tasks and its strength in data processing often relies on highly optimized C libraries, mitigating the direct
  performance comparison in those contexts.
